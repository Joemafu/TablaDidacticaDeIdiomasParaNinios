import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'tabla.didactica.de.idiomas',
  appName: 'TablaDidacticaDeIdiomasParaNinios',
  webDir: 'www'
};

export default config;
